import React from 'react'

/**
 * @returns A page that lets the user know they have come across a page they do not have access to
 */
export default function AccessDenied() {
    return (
        <div>
            Access Denied
        </div>
    )
}
